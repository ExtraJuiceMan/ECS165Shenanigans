A pre-compiled x86_64 Windows and Linux build has been included.

If they don't work for you, then build...

You must have the Rust toolchain installed to build.
https://www.rust-lang.org/learn/get-started

To build and run from root directory:

cd lstore
python3 -m venv .env
./.env/bin/activate (or equivalent on other OS)
pip install maturin
maturin develop --release
python ../__main__.py

